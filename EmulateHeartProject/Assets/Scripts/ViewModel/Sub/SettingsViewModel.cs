public class SettingsViewModel : ISceneViewModel
{
    /*
    private int _gameVolume;
    private int _seVolume;
    private int _textSpeed;
    private bool _saveConfirmation;
    private int _geminiModel;
    private string _geminiApiKey;

    public int GameVolume => _gameVolume;
    public int SeVolume => _seVolume;
    public int TextSpeed => _textSpeed;
    public bool SaveConfirmation => _saveConfirmation;
    public int GeminiModel => _geminiModel;
    public string GeminiApiKey => _geminiApiKey;

    public SettingsViewModel(SettingsData settingsData)
    {
        _gameVolume = Mathf.RoundToInt(settingsData.GameVolume * 100);
        _seVolume = Mathf.RoundToInt(settingsData.SeVolume * 100);
        _textSpeed = Mathf.RoundToInt(settingsData.TextSpeed * 100);
        _saveConfirmation = settingsData.SaveConfirmation;
        _geminiModel = (int)settingsData.GeminiModel;
        _geminiApiKey = settingsData.GminiApiKey;
    }
    */
}
